---
interface Props {
  href: string;
  title: string;
  description: string;
  order: number;
  duration?: string;
}

const { href, title, description, order, duration } = Astro.props;
---

<a class="card" href={href}>
  <div class="card-top">
    <span class="tag">Charla {order}</span>
    {duration && <span>{duration}</span>}
  </div>
  <h2>{title}</h2>
  <p>{description}</p>
  <div class="card-foot">
    <span>Recordar</span>
    <span class="card-arrow" aria-hidden="true">→</span>
  </div>
</a>
